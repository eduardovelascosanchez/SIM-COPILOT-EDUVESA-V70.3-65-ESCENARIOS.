export function downloadReport(s,r){const t=["SIMCOPILOT EDUVESA V7.0",`Instructor: ${s.meta.instructor||"No registrado"}`,`Equipo: ${s.meta.team||"No registrado"}`,`Escenario: ${s.scenario.title}`,`Duración: ${fmt(s.elapsed)}`,`Puntuación: ${r.score}/100`,`Decisión: ${r.decision}`,"","CRONOLOGÍA",...s.log.slice().reverse().map(e=>`${e.time} — ${e.action}`)].join("\n");const b=new Blob([t],{type:"text/plain"}),a=document.createElement("a");a.href=URL.createObjectURL(b);a.download="reporte-simcopilot.txt";a.click()}export function printReport(s,r){const w=open("");w.document.write(`<h1>SimCopilot EDUVESA V7.0</h1><p>${s.scenario.title}</p><p>Puntuación: ${r.score}/100 · ${r.decision}</p><ol>${s.log.slice().reverse().map(e=>`<li>${e.time} — ${e.action}</li>`).join("")}</ol>`);w.document.close();w.print()}const fmt=s=>`${String(Math.floor(s/60)).padStart(2,"0")}:${String(s%60).padStart(2,"0")}`